# Scratch notes

Freeform. Never graded.

- Lab 11 already logs the confident-wrong Bayes case. Quote the criterion
  name (`confident_wrong`) if you want the analysis to sound like you
  were there.
- `None` is a better API than `np.zeros(2)` for "I do not know." Zero
  looks like a measurement.
- The durable artifact is `tests/test_lab23_assumption.py`. That is the
  law you are writing for your own stack.
