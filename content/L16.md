---
id: L16
concepts: [pose graph, loop closure, Gauss-Newton]
prereqs: [L14, L15]
lab_id: lab15
---

SLAM, refused as FastSLAM: one 2-D pose graph, one loop closure, one Gauss-Newton loop.

Edges $z_{i,i+1}$ drift. The last pose *should* be the first. Error $e=\ominus(z,\ominus(x_i,x_j))$; hold $x_0$ fixed. ATE drop $\ge 60\%$ after the loop is the check. The picture is a square that does not close, then snaps.
