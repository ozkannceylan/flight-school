# Lecture notes

Derivations that were kept out of the lab READMEs. One file per lecture,
fixed frontmatter so `tools/studypack.py` is a concatenator, not a parser
with opinions.

```yaml
---
id: L01
concepts: [state, integrator]
prereqs: []
lab_id: lab00
---
```

`make studypack` writes `build/studypack_phaseN.md` (gitignored). Direction
is repo → artifacts. Nothing generated writes back here.
